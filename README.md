# Mini-Shell-
Un shell bash simple que j'ai codé en C pour Linux 

après installation compiler le script avec la commande suivante : 

gcc minishell.c -o minishell 

puis lancer le programme avec la commande suivante 

./minishell
